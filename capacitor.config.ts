import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'notif mute system',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
